name: Docker Image CI

on:
  push:
    branches:
      - master

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set lowercase image name
        run: |
          echo "IMAGE_NAME_LC=$(echo ${GITHUB_REPOSITORY} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set tag
        run: |
          echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Build image
        run: |
          docker build \
            -t $REGISTRY/$IMAGE_NAME_LC:$IMAGE_TAG \
            -t $REGISTRY/$IMAGE_NAME_LC:latest \
            .

      - name: Push image
        run: |
          docker push $REGISTRY/$IMAGE_NAME_LC:$IMAGE_TAG
          docker push $REGISTRY/$IMAGE_NAME_LC:latest

      - name: Run migrations
        run: |
          echo "Ready!"
#        run: |
#          docker run --rm \
#            -e DATABASE_URL="${{ secrets.STAGING_DATABASE_URL }}" \
#            -e REDIS_URL="${{ secrets.STAGING_REDIS_URL }}" \
#            -e SECRET_KEY="${{ secrets.STAGING_SECRET_KEY }}" \
#            -e APP_ENV=staging \
#            -e APP_VERSION=${GITHUB_SHA::7} \
#            -e SERVICE_NAME=pirksp \
#            ghcr.io/${IMAGE_NAME_LC}:${IMAGE_TAG} migrate
